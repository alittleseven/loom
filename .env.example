# 织机 Loom —— edge 层 LLM API Key 模板
# 用法：复制为 .env 后填写。.env 已被 .gitignore 覆盖，真实密钥严禁提交。
# 模型路由表（渲染档/评审档/scribe 档/小档）的最终形态随 P1b 盲测定版，字段可能调整。

# 通用 Key（如各档位共用同一供应商）
LOOM_LLM_API_KEY=

# 按档位区分（可选，v3.0 方案 §5.3 三档路由）
LOOM_LLM_API_KEY_ECON=
LOOM_LLM_API_KEY_MID=
LOOM_LLM_API_KEY_STRONG=
